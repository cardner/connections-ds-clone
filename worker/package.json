{
  "name": "connections-ds-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that proxies the NYT Connections puzzle into a compact, DS-parseable format over plain HTTP.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260714.1",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
